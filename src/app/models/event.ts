export class Event {
    id: number;
    circleId: number;
    whereType: string;
    whoStag: boolean; 
    whoLady: boolean; 
    whoClergy: boolean; 
    //whoFamily: boolean; 
    what: string;
    when: Date;
    where: string;
    why: string;
}